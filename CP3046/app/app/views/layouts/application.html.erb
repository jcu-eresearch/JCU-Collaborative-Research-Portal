<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= yield(:head) %>
</head>
<body>

<% if logged_in? %>
	<%= button_to "Logout", :controller => "session", :method => :delete %>
<% else %> 
	<% # button_to 'Login', :controller => "session", :method => :create %>
<% end %>

<%= yield %>

</body>
</html>
